<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.invoice.mapper.OperationLogMapper">
    <insert id="insert" parameterType="com.example.invoice.entity.OperationLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_operation_log (user_id, username, operation_type, module, target_id, target_no,
            content, old_value, new_value, client_ip, user_agent)
        VALUES (#{userId}, #{username}, #{operationType}, #{module}, #{targetId}, #{targetNo},
            #{content}, #{oldValue}, #{newValue}, #{clientIp}, #{userAgent})
    </insert>

    <select id="selectByCondition" resultType="com.example.invoice.entity.OperationLog">
        SELECT * FROM sys_operation_log
        <where>
            <if test="module != null and module != ''">AND module = #{module}</if>
            <if test="operationType != null and operationType != ''">AND operation_type = #{operationType}</if>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="startDate != null and startDate != ''">AND created_at >= #{startDate}</if>
            <if test="endDate != null and endDate != ''">AND created_at &lt;= #{endDate}</if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByCondition" resultType="int">
        SELECT COUNT(*) FROM sys_operation_log
        <where>
            <if test="module != null and module != ''">AND module = #{module}</if>
            <if test="operationType != null and operationType != ''">AND operation_type = #{operationType}</if>
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="startDate != null and startDate != ''">AND created_at >= #{startDate}</if>
            <if test="endDate != null and endDate != ''">AND created_at &lt;= #{endDate}</if>
        </where>
    </select>
</mapper>
