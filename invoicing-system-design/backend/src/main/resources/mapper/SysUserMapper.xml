<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.invoice.mapper.SysUserMapper">
    <select id="selectByUsername" parameterType="string" resultType="SysUser">
        SELECT * FROM sys_user WHERE username = #{username} AND status = 1
    </select>
    <select id="selectById" parameterType="long" resultType="SysUser">
        SELECT * FROM sys_user WHERE id = #{id}
    </select>
    <select id="selectAll" resultType="SysUser">
        SELECT * FROM sys_user ORDER BY id
    </select>
    <insert id="insert" parameterType="SysUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, real_name, role, status, created_at, updated_at)
        VALUES (#{username}, #{password}, #{realName}, #{role}, #{status}, #{createdAt}, #{updatedAt})
    </insert>
    <update id="update" parameterType="SysUser">
        UPDATE sys_user
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = #{updatedAt}
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById" parameterType="long">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>
</mapper>